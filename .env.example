# Supabase Configuration
# Get these values from https://app.supabase.com/project/_/settings/api

# Your Supabase project URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase anonymous (public) key
# This is safe to use in client-side code
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# IMPORTANT SECURITY NOTES:
# 1. Never commit the actual .env file to version control
# 2. The anon key is safe for client-side use (RLS protects your data)
# 3. Never expose your service_role key in client-side code
# 4. Set these values in your deployment platform (Vercel, Netlify, etc.)
